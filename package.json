{
  "name": "pub-finder",
  "private": true,
  "description": "Static pub finder site -- no build step for deployment, TypeScript is used only to type-check the plain JS via JSDoc.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "typescript": "^5.6.3"
  }
}
